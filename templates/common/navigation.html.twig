<nav class="navbar navbar-expand navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('home') }}">&nbsp Library.io Wishlist Task</a>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item{% if app.request.get('_route') == 'catalogue' %} active{% endif %}">
                <a class="nav-link" href="{{ path('catalogue') }}">Catalogue</a>
            </li>

            <li class="nav-item{% if app.request.get('_route') == 'wishlist' %} active{% endif %}">
                <a class="nav-link" href="{{ path('wishlist') }}">Wishlist</a>
            </li>

            {% if not is_granted('ROLE_USER') %}
                <li class="nav-item{% if app.request.get('_route') == 'login' %} active{% endif %}">
                    <a class="nav-link" href="{{ path('login') }}">Login</a>
                </li>
            {% endif %}

            {% if not is_granted('ROLE_USER') %}
                <li class="nav-item{% if app.request.get('_route') == 'register' %} active{% endif %}">
                    <a class="nav-link" href="{{ path('register') }}">Register</a>
                </li>
            {% endif %}

            {% if is_granted('ROLE_USER') %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_logout') }}">Log out</a>
            </li>
            {% endif %}
        </ul>
    </div>
</nav>